# Optional. Every stage falls back to the default baked into its script.
# See `python main.py list` for the stages and `--set NAME=VALUE` to override ad hoc.

FC_PROXY_FILE=decodo_scraper_ips.txt
FC_NUM_THREADS=30
FC_SLEEP=0.4
FC_PAGE_SIZE=30
